# Push to GitHub

Your code is ready and committed locally! To push to GitHub, follow these steps:

## Option 1: Using Terminal (Recommended)

```bash
cd /Users/next/business-scraper
git push -u origin main
```

You'll be prompted for your GitHub credentials.

## Option 2: Using GitHub CLI

If you have GitHub CLI installed:

```bash
cd /Users/next/business-scraper
gh auth login
git push -u origin main
```

## Option 3: Using SSH

If you prefer SSH:

```bash
cd /Users/next/business-scraper
git remote set-url origin git@github.com:fring-12/business-scraper.git
git push -u origin main
```

## What's Already Done ✅

- ✅ Git repository initialized
- ✅ All files committed locally
- ✅ Remote repository configured
- ✅ Branch set to 'main'

## What You Need to Do

Just run the push command and enter your GitHub credentials when prompted!

After pushing, your code will be live at:
**https://github.com/fring-12/business-scraper**

